namespace MobaVR
{
    public enum ClassicModeState
    {
        MODE_INIT,
        MODE_INACTIVE,
        MODE_START,
        
        ROUND_READY,
        ROUND_PLAY,
        ROUND_PAUSE,
        ROUND_CANCEL,
        ROUND_COMPLETE,
        
        MODE_COMPLETE
    }
}